<Page
    x:Class="UWP.JoinUs"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0"  Margin="0,5,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Uid="Feedback" x:Name="Feedback"
                    Style="{ThemeResource ButtonRevealStyle}"
                    Content="Feedback" Click="Feedback_Click" Margin="5,0,5,0" />
            <Button Grid.Column="1" x:Uid="Contribute" x:Name="Contribute" 
                    Style="{ThemeResource ButtonRevealStyle}"
                    Content="Contribute" Click="Contribute_Click" Margin="5,0,5,0" />
        </Grid>
    </Grid>
</Page>

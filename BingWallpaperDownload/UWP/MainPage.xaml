<Page
    x:Class="UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Margin="10,10,10,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,5,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" x:Uid="Download" Content="Download" Click="DownloadButton_Click" Margin="5,0,5,0"/>
            <Button Grid.Column="1" x:Uid="OpenBing" Content="Open Bing" Click="OpenBingButton_Click" Margin="5,0,5,0"/>
        </Grid>
        <Grid Grid.Row="1" Margin="5,5,0,5">
            <TextBlock x:Name="MainHint" HorizontalAlignment="Left" Text="Waiting" TextWrapping="Wrap"  Visibility="Visible"/>
        </Grid>
        <Grid Grid.Row="2">
            <HyperlinkButton x:Name="ImageTodayHrefButton"
                             AutomationProperties.Name="Go to Bing for image today">
                <Image x:Name="ImageToday" Width="auto" Height="auto"/>
            </HyperlinkButton>
            <StackPanel Height="40" VerticalAlignment="Bottom" 
                        Background="DarkGray" Opacity=".75">
                <TextBlock x:Name="ImageTodayTitle" Width="auto" Height="auto"
                           FontSize="24" Text="Title" TextWrapping="Wrap"/>
                <TextBlock x:Name="ImageTodayDescription" Width="auto" Height="auto"
                           FontSize="18" Text="Description" TextWrapping="Wrap"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
